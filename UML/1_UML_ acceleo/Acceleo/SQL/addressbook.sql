CREATE DATABASE addressbook;

CREATE TABE Adresse(
		numero Integer,
		rue VARCHAR(255)
	);
CREATE TABE Personne(
		nom VARCHAR(255),
		prenom VARCHAR(255),
		age Integer,
		identifier VARCHAR(255)
	);
CREATE TABE AdressBook(
		name VARCHAR(255),
	);
